<h1>These are some cool duckys:</h1>

<ul>
  <% @ducks.each do |duck| %>
    <li> <%= link_to duck.name, duck_path(duck) %> </li>
  <% end %>
</ul>

<button><%= link_to "Create Duck", new_duck_path%> </button>
